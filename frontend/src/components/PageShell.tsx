export const PageShell = ({ children }: { children: React.ReactNode }) => {
  return <div className="mx-auto flex w-full min-w-0 max-w-7xl flex-col gap-6 px-4 pb-16 pt-6 sm:px-6 lg:px-8">{children}</div>
}
